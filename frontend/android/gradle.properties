org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# 禁止 Flutter 插件强制注入全部 ABI（native 后端 libbackend_shared.so 只有
# arm64-v8a/x86_64 两档，32 位 ABI 无后端可用），
# 由 app/build.gradle.kts 的 ndk.abiFilters 自行控制
disable-abi-filtering=true
# 项目路径含空格（Program Files）时 Kotlin 增量编译缓存会损坏，禁用之
kotlin.incremental=false
# 构建走本地 Gradle 缓存+代理；保留守护进程（W4-4 去 Chaquopy 后此旗标仅余常规作用）
org.gradle.daemon=true
